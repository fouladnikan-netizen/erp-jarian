/**
 * tasks — پویش (Activity + Task). Distinct aggregates (DDL-15 / DDL-16).
 * Application files remain at backend/src/services/{task,activity}* until Phase 2.1.
 */
export { MODULE_ID } from './ownership.js';
