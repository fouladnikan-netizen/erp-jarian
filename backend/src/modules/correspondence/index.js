/**
 * correspondence — دبیرخانه / گاه‌شمار.
 * Domain moved here. HTTP still `/api/v1/correspondence*` via existing routes
 * until Phase 2.1 application move.
 */
export { MODULE_ID } from './ownership.js';
