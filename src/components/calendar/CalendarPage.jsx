import CommitmentEngine from './CommitmentEngine';

/**
 * گاه‌شمار — موتور تعهدات (صفحه مستقل سایدبار، مسیر /gahshomar).
 * تقویم شبکه‌ای هدر جداست و از آیکون هدر به‌صورت مودال باز می‌شود.
 */
export default function CalendarPage() {
  return <CommitmentEngine />;
}
